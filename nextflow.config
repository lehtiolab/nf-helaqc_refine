process.container = 'lehtiolab/helaqc:2.0dev'


profiles {

  standard {
    includeConfig 'configuration/base.config'
  }
  qc {
    includeConfig 'configuration/base.config'
    process {
      executor = 'slurm'
      clusterOptions = '--qos=qc'
    }
  }
  docker {
    docker.enabled = true
    docker.runOptions = '-u $(id -u):$(id -g)'
    docker.runOptions = '--rm'
  }
  test {
    includeConfig 'configuration/base.config'
    includeConfig 'configuration/test.config'
    executor {
      cpus = 20
    }
  }
}
